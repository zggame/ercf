# Curated Dataset Layout

- `tmp/datasets/freddie_mac/<snapshot>.json`
- `tmp/datasets/fannie_mae/<snapshot>.json`

Each file contains canonical loan-level rows with `source`, `snapshot`, core loan metrics, and precomputed refined-calculation outputs.
